# crazy_ball

### Quick start guide

Clone the repository, then run in the clonned directory the next commands:

```
mkdir build
cd build
cmake ..
cmake --build .
./main
```

### Controls

`←` - **move to the left**

`→` - **move to the right**

`Space` - **jump**